name: Smartwear sklad feed

on:
  schedule:
    - cron: '55 */4 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install deps
        run: pip install lxml

      - name: Generate feed
        env:
          SMARTWEAR_XML_URL: ${{ secrets.SMARTWEAR_XML_URL }}
        run: |
          mkdir -p docs
          python3 smartwear_sklad_feed.py - docs/smartwear_sklad_shoptet.xml

      - name: Commit
        run: |
          git config user.name "bot"
          git config user.email "bot@users.noreply.github.com"
          git add docs/smartwear_sklad_shoptet.xml docs/smartwear_pamat_kodov.txt
          git diff --staged --quiet || git commit -m "smartwear sklad update"
          git push
